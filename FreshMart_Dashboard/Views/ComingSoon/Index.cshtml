@{
    ViewBag.Title = "Demo";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
</style>

<div class="container">
    <div class="row">
        <div class="col-12 col-md-12 col-xs-12 col-lg-12">
            <div class="smart-shop__img">
                @*<canvas style="background-image: url(../Assets/img/product_group/smart-shop.jpg);background-repeat:no-repeat;width:100%;height:100%"></canvas>*@

                <img src="../Assets/img/product_group/smart-shop.jpg" />
                <div class="smart-shop__btn btn1">
                    <button type="button" class="btn btn__pulse">
                        <span><i class="fa fa-plus"></i></span>
                    </button>
                    @*<div class="smart-shop__btn--details">
                            <a href="javascript:void(0)">
                                Minimal Frame
                            </a>
                            <span class="price">
                                <strong>$250.00</strong>
                                <span class="old">$450.00</span>
                            </span>
                        </div>*@
                </div>
                <div class="smart-shop__btn btn2">
                    <button type="button" class="btn btn__pulse">
                        <span><i class="fa fa-plus"></i></span>
                    </button>

                </div>
                <div class="smart-shop__btn btn3">
                    <button type="button" class="btn btn__pulse">
                        <span><i class="fa fa-plus"></i></span>
                    </button>

                </div>
            </div>
        </div>
    </div>
</div>



<script>
    $(document).ready(function () {
        $("img").on("click", function (event) {
            var x = event.pageX - this.offsetLeft;
            var y = event.pageY - this.offsetTop;
            alert("X Coordinate: " + x + " Y Coordinate: " + y);
        });


    });

    function getMousePosition(canvas, event) {
        let rect = canvas.getBoundingClientRect();
        let x = event.clientX - rect.left;
        let y = event.clientY - rect.top;
        console.log("Coordinate x: " + x,
            "Coordinate y: " + y);
    }

    let canvasElem = document.querySelector("img");

    canvasElem.addEventListener("mousedown", function (e) {
        getMousePosition(canvasElem, e);
    });
</script>